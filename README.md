A demo app made using express w/ view rendering via pug

To get up and running do the following:
```shell
$ git clone git@github.com:egoberdon/demo.git
$ cd demo
$ npm install
$ DEBUG=myapp:* npm start
```